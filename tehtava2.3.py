import math

kanta = float(input('Anna kanta: '))
korkeus = float(input('Anna korkeus: '))
piiri = float(2 * kanta + 2 * korkeus)
pintaala = float(kanta * korkeus)

print(f'Piiri on {piiri}')
print(f'Pinta-ala on {pintaala}')
